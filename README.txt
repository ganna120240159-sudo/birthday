# Loki Birthday Transmission

A funny birthday website designed as a fake transmission from the future.

## Customize

Open `script.js` and edit the message inside `showMessage()`.

You can also replace the fake memory cards in `index.html` with your own photos.

## Publish

Upload all files to a GitHub repository and enable GitHub Pages.
